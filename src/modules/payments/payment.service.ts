export const confirm = async (orderId: string, userId: string) => {
  return { message: "Service coming soon" };
};

export const webhook = async (data: any) => {
  return { message: "Service coming soon" };
};
